import { defineConfig } from 'vite'

export default defineConfig({
    // base: '/repo-name/' // TODO: Update this with the actual repo name
    base: './', // Use relative base for now so it works locally and on simple hosts
})
